# day-07.md ― Day 7 進行表

> 進行AIはセッション開始時に `docs/CONTEXT.md` → このファイル → 今日の opening の順に読む（`content/README.md` §1）。

| 項目 | 内容 |
|---|---|
| 対象 | LEVEL 3 TIME MACHINE |
| 今日のレッスン | `MISSION-03-7` 同じ操作をCLIで再現する |
| 合計時間 | 25分 |
| XP | 100 |
| 備考 | **短い日。** 前日までが重い2本（⚠×2）だったので、疲れていたらここで区切る |

## 進行

| # | レッスン | 送るopening | 内部台本 | 時間 |
|---|---|---|---|---|
| 1 | Mission 3-7 同じ操作をCLIで再現する | `content/level03/opening-03-7.md` | `content/level03/mission-03-7.md` | 25分 |

## 今日の到達点

- **`notes/level03-cli.md`**（GUI↔CLI 対応表・学習者が自分で書いたもの）
  - ※このファイルは BOSS-01 で「削除される側」の素材になる。**必ずPushまで済ませておく**
- `notes/log/{今日の日付}.md` に成長ログ

## 打ち切り基準

- STEP 2（4コマンドを打つ）まで終われば、STEP 4（対応表を書く）は翌日に回してよい。
- ただし **`notes/level03-cli.md` が存在しないまま BOSS-01 に進まない**（Bossの完了条件に「復活していること」が入っているため）。

## 進行AIへの注意

- **目標は「読める」であって「暗記」ではない。** 冒頭で明言する。
- 扱うのは読み取り系4つだけ：`git status` / `git log --oneline` / `git branch` / `git diff`。**これ以外は打たせない。**
- `git reset` / `git checkout <ハッシュ>` / `rm` / `git push --force` は出さない（危険なコマンドはLEVEL 9 の 9-9 で扱う）。
- Day1・Day2 でターミナルを一度も使っていない可能性が高い。**「打ち間違えても何も壊れないコマンドだけを使います」**を最初に明言する。
- 対応表は教材が渡さない。**学習者が自分の言葉で書く**（丸写しになっていたら聞き返す）。
- STEP 2 の練習用の変更（README.mdへの1行追加）は、最後に `Discard changes` で必ず戻す。
